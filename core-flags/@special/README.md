# Special Node Layer key indication

All special layers are by default, ignored on the result data.

## `@ignore` - Ignore All indication

## Features : Ignore

> ignores node with names, or plugin data
